═══════════════════════════════════════════════════════════════
📖 CADERNO DE ERROS E ACERTOS - PLATAFORMA DE REVISÃO TEOT
═══════════════════════════════════════════════════════════════

**Este arquivo registra erros resolvidos, acertos e problemas conhecidos.**
**SEMPRE ADICIONAR ao final, NUNCA sobrescrever completamente.**

═══════════════════════════════════════════════════════════════
📅 REGISTRO DE CHATS
═══════════════════════════════════════════════════════════════

**CHAT INICIADO:** 2025-11-29 (Sexta-feira) ~17:00
**CHAT ENCERRADO:** 2025-11-29 (Sexta-feira) 17:57

**CHAT INICIADO:** 2025-12-02 (Terça-feira) 22:28

**CHAT INICIADO:** 2025-12-02 (Terça-feira) 23:35

**CHAT INICIADO:** 2025-12-11 (Quinta-feira) 18:30

**CHAT INICIADO:** 2025-12-12 (Sexta-feira) ~22:06
**CHAT ENCERRADO:** 2025-12-12 (Sexta-feira) ~22:06

**CHAT INICIADO:** 2025-12-12 (Sexta-feira) 22:08

═══════════════════════════════════════════════════════════════
✅ ACERTOS E SOLUÇÕES BEM SUCEDIDAS
═══════════════════════════════════════════════════════════════

**2025-11-29:**
- ✅ Criação rápida e eficiente de HTMLs interativos (dificuldade 2/10)
- ✅ Sistema de validação automatizada com script Python
- ✅ Adaptação fácil de cores usando variáveis CSS
- ✅ Estrutura reutilizável para criação de novos documentos HTML
- ✅ Documentação técnica completa para facilitar replicação

═══════════════════════════════════════════════════════════════
❌ ERROS RESOLVIDOS
═══════════════════════════════════════════════════════════════

**2025-12-02:**
- ✅ **PROBLEMA CRÍTICO RESOLVIDO:** Itens na aba "Pendências" não respondiam ao clique/toque
  - **Causa:** IDs inválidos (undefined/null) ou elementos filhos interceptando eventos
  - **Solução implementada:**
    1. Validação de ID antes de renderizar (garantir que sempre seja string válida)
    2. Substituição de onclick inline por data-attribute + event delegation
    3. Adição de `pointer-events: none` em elementos filhos para evitar interceptação
    4. Validação robusta na função togglePendencia() com conversão consistente para string
    5. Event delegation no container para garantir funcionamento mesmo com elementos filhos
  - **Itens afetados:** Fratura de clavícula, Epifisiolistese, Sd manguito rotador, DDQ, Luxação e Instabilidade do cotovelo, LAC/LEC, Epicondilites, Fraturas do cotovelo
  - **Prevenção:** Validação de ID sempre que renderizar, uso de event delegation, proteção contra IDs inválidos

- ✅ **CORREÇÃO ADICIONAL (02/12/2025):** Event listener sendo adicionado múltiplas vezes causando travamentos
  - **Causa:** Event listener era adicionado toda vez que `renderPendencias()` era chamado, criando múltiplos listeners no mesmo elemento
  - **Solução:** Criada função `inicializarEventListenersPendencias()` que adiciona o listener apenas uma vez durante inicialização, removendo listener antigo se existir
  - **Prevenção:** Sempre usar event delegation com função nomeada que pode ser removida, adicionar listener apenas uma vez durante inicialização

- ✅ **CORREÇÃO CRÍTICA MOBILE (02/12/2025):** Mudança para onclick inline (solução mais simples e que funciona)
  - **Causa:** Event delegation com touchend não estava funcionando no iPhone. Tentativa anterior de adicionar listeners programaticamente falhou
  - **Solução:** Mudado para usar `onclick` inline diretamente no HTML, igual às tabs que funcionam. Removida função `inicializarEventListenersPendencias()` e event delegation complexa
  - **Lição:** Quando algo não funciona, voltar ao básico. Se tabs funcionam com onclick inline, usar a mesma abordagem. Não complicar com event delegation quando onclick inline resolve
  - **Prevenção:** SEMPRE usar a solução mais simples primeiro. Se outros elementos similares funcionam com onclick inline, usar a mesma abordagem antes de tentar soluções complexas

- ✅ **CORREÇÃO DEFINITIVA (02/12/2025) - DIAGNÓSTICO OPUS:** IDs com underscore causavam erro de sintaxe JavaScript no onclick
  - **Causa Raiz Identificada pelo Opus:** IDs gerados sem valor no CSV recebem formato `${Date.now()}_${lineNum}` (ex: "1733174400000_5"). Quando usado sem aspas no onclick: `onclick="togglePendencia(1733174400000_5)"`, isso é sintaxe JavaScript inválida (underscore não é válido em literal numérico). Navegador ignora silenciosamente o onclick mal formado.
  - **Por que alguns funcionavam:** Itens com ID numérico puro no CSV funcionavam. Itens sem ID (que recebiam ID gerado com underscore) falhavam.
  - **Solução:** Sempre usar aspas no onclick para tratar ID como string: `onclick="togglePendencia('${temaIdStr}')"`. Isso funciona tanto para números quanto strings com underscore.
  - **Lição:** Sempre usar aspas em valores interpolados em atributos HTML que serão interpretados como JavaScript. Nunca assumir que valores são sempre números. Erros de sintaxe JavaScript em atributos HTML são ignorados silenciosamente pelo navegador.
  - **Prevenção:** SEMPRE usar aspas ao interpolar valores em atributos onclick/on* que serão interpretados como JavaScript. Validar formato de IDs gerados automaticamente.

- ⚠️ **PROBLEMA PERSISTENTE (03/12/2025) - iOS Safari PWA:** Alguns itens na aba Pendências não respondem ao toque no iPhone, mesmo após múltiplas correções
  - **Comportamento:** No MacBook (Chrome/Safari) funciona perfeitamente. No iPhone (Safari iOS, PWA instalado) alguns itens específicos não respondem ao toque, enquanto outros funcionam normalmente.
  - **Itens afetados:** Fratura de clavícula, Epifisiolistese, Sd manguito rotador, DDQ, Luxação e Instabilidade do cotovelo, LAC/LEC, Epicondilites, Fraturas do cotovelo
  - **Tentativas realizadas:**
    1. Event delegation no container
    2. Onclick inline com aspas simples: `onclick="togglePendencia('${id}')"`
    3. Escape de aspas com `replace(/'/g, "\\'")`
    4. Event delegation como fallback (touchend + click)
    5. Remoção de verificação que bloqueava event delegation
    6. Atualização agressiva do Service Worker (limpar caches, desregistrar SWs antigos)
    7. Limpeza manual de cache no Safari (Ajustes → Safari → Avançado)
    8. ❌ **TENTATIVA INCORRETA:** Uso de `JSON.stringify()` no onclick - gera HTML inválido
  - **Status:** PROBLEMA PERSISTE - Enviado para consulta externa (GPT/Opus) com caso técnico completo
  - **Arquivos de referência:** `docs/CASO_CONSULTA_EXTERNA_FINAL.md`, `diagnostico_final.py`
  - **Observação importante:** Código funciona perfeitamente no MacBook, indicando que problema é específico do iOS Safari/PWA, não do código em si

- ✅ **BUG CRÍTICO DESCOBERTO E CORRIGIDO (03/12/2025):** JSON.stringify no onclick gera HTML inválido
  - **Causa Raiz:** `JSON.stringify(temaIdStr)` retorna string COM aspas duplas: `"\"1733174400000_5\""`. Quando interpolado em `onclick="togglePendencia(${json})"`, gera HTML inválido: `onclick="togglePendencia("1733174400000_5")"` - as aspas duplas fecham o atributo prematuramente.
  - **Por que não foi detectado antes:** Navegadores desktop podem ser mais permissivos com HTML mal formado. iPhone (PWA) é mais estrito.
  - **Solução aplicada:** Reverter para escape manual com aspas simples: `temaIdStr.replace(/'/g, "\\'").replace(/\\/g, "\\\\")` e usar `onclick="togglePendencia('${temaIdEscaped}')"`. Isso gera HTML válido: `onclick="togglePendencia('1733174400000_5')"`.
  - **Lição:** JSON.stringify é útil para JSON, mas NÃO para interpolação em atributos HTML. Aspas duplas retornadas por JSON.stringify quebram atributos HTML que usam aspas duplas. Sempre usar escape manual + aspas simples para atributos onclick.
  - **Prevenção:** NUNCA usar JSON.stringify diretamente em atributos HTML. Sempre usar escape manual + aspas simples. Testar geração de HTML com script antes de implementar.
  - **Arquivos:** `docs/DESCOBERTA_BUG_JSON_STRINGIFY.md`, `analise_json_stringify.py`

═══════════════════════════════════════════════════════════════
⚠️ PROBLEMAS CONHECIDOS (AINDA NÃO RESOLVIDOS)
═══════════════════════════════════════════════════════════════

[SERÁ ATUALIZADO COM PROBLEMAS IDENTIFICADOS]

═══════════════════════════════════════════════════════════════
💡 LIÇÕES APRENDIDAS
═══════════════════════════════════════════════════════════════

**2025-11-29:**
- 💡 Criação de HTML interativo é uma das tarefas mais leves e rápidas (2/10 de dificuldade)
- 💡 Ter um template/estrutura pronta facilita muito a criação de novos documentos
- 💡 Validação automatizada garante qualidade e consistência
- 💡 Documentação técnica bem feita permite replicação rápida em outros projetos
- 💡 Organização e estrutura são fundamentais para criação eficiente
- 💡 HTML standalone é melhor para compartilhamento do que integração direta

**2025-12-02:**
- 💡 **Event delegation é mais robusto que onclick inline** - Funciona mesmo com elementos filhos e evita problemas com IDs inválidos
- 💡 **Sempre validar IDs antes de usar** - IDs undefined/null podem quebrar silenciosamente funções JavaScript
- 💡 **Consistência de tipos no Set() é crítica** - Garantir que sempre use o mesmo tipo (string) para comparações funcionarem
- 💡 **pointer-events: none em elementos filhos** - Evita que elementos filhos interceptem eventos de clique do elemento pai
- 💡 **Problemas intermitentes podem ser causados por dados inconsistentes** - Alguns itens funcionam e outros não geralmente indica problema com dados, não código
- 💡 **NUNCA adicionar event listeners dentro de funções de renderização** - Isso cria múltiplos listeners que causam travamentos e comportamentos estranhos. Sempre adicionar listeners apenas uma vez durante inicialização
- 💡 **Usar funções nomeadas para event handlers quando precisar remover depois** - Permite remover listeners antigos antes de adicionar novos, evitando acúmulo
- 🚨 **CRÍTICO: PLATAFORMA É USADA PRINCIPALMENTE NO IPHONE - SEMPRE TESTAR/AJUSTAR PARA MOBILE PRIMEIRO!** - Usuário usa principalmente pelo iPhone, não desktop. Sempre adicionar suporte explícito para eventos touch (touchend) além de click. Não assumir que funciona em mobile só porque funciona em desktop. Este erro já aconteceu múltiplas vezes - SEMPRE pensar em mobile primeiro!

**2025-12-03:**
- ✅ **PROBLEMA CRÍTICO RESOLVIDO:** renderCaderno criava anotações vazias que sobrescreviam importadas
  - **Causa:** `renderCaderno()` chamava `obterOuCriarAnotacao()` para TODOS os temas, criando anotações vazias que sobrescreviam as importadas
  - **Solução:** Removido `obterOuCriarAnotacao()` automático. Agora só cria anotação quando usuário realmente edita
  - **Prevenção:** NUNCA criar dados automaticamente em funções de renderização. Só criar quando usuário interage explicitamente
  - **Lição:** Renderização deve ser READ-ONLY. Não deve criar/modificar dados, apenas exibir

- ✅ **PROBLEMA CRÍTICO RESOLVIDO:** Aba Pendências travando por múltiplos event listeners
  - **Causa:** `togglePendencia()` chamava `inicializarEventListenersPendencias()` após cada toggle, criando múltiplos listeners que causavam travamentos
  - **Solução:** Removida função `inicializarEventListenersPendencias()` completamente. Voltado para onclick inline (mesma solução das outras abas que funcionam)
  - **Prevenção:** NUNCA re-inicializar event listeners dentro de funções que são chamadas frequentemente. Se outras abas funcionam com onclick inline, usar a mesma abordagem
  - **Lição:** Simplicidade > Complexidade. Se onclick inline funciona nas outras abas, usar a mesma solução. Não complicar com event delegation quando não é necessário

**2025-12-12:**
- 🎉 **VITÓRIA COMPLETA: Sistema de Atualização Automática para PWA no iPhone**
  - **Problema:** iPhone não atualizava automaticamente quando havia mudanças. Usuário tinha que fechar e reabrir app manualmente. Service Worker cached não detectava novas versões. Frustração constante: "sempre na mesma lama".
  - **Solução Implementada:**
    1. Sistema de detecção automática de novas versões via Service Worker
    2. Notificação visual quando há atualização disponível ("🔄 Nova versão disponível!")
    3. Botão "Atualizar Agora" para forçar atualização imediata
    4. Limpeza automática de cache antigo ao iniciar
    5. Verificação periódica de atualizações (a cada 1 minuto)
    6. Versionamento correto do Service Worker (`CACHE_NAME`)
  - **Resultado:** ✅ **FUNCIONOU PERFEITAMENTE!** Botão apareceu no iPhone, usuário atualizou sem problemas. **"ETAPA VENCIDA E SUPERADA!"**
  - **Lições Críticas:**
    1. **Push para GitHub é ESSENCIAL** - Sem push, iPhone nunca atualiza. GitHub Pages serve versão do repositório.
    2. **Versionamento do Service Worker** - Mudar `CACHE_NAME` força detecção de nova versão. Formato: `vrvs-v5.X-feature-YYYYMMDD`.
    3. **Notificação visual funciona** - UX melhor que atualização silenciosa. Usuário vê claramente quando há atualização.
    4. **Processo completo necessário** - Código → Commit → **Push** → Teste. Não adianta só mudar código localmente.
  - **Processo Correto para Futuro:**
    1. Fazer mudanças no código
    2. Atualizar `CACHE_NAME` no `sw.js`
    3. Atualizar versão no `index.html` (`APP_VERSION`, `SW_VERSION`)
    4. Commit das mudanças
    5. **PUSH PARA GITHUB** ← CRÍTICO!
    6. Aguardar 1-2 minutos (GitHub Pages atualiza)
    7. Testar no iPhone (deve aparecer notificação)
  - **Prevenção:** NUNCA esquecer o push. Sempre atualizar `CACHE_NAME` quando mudar código. Sistema agora detecta automaticamente e notifica usuário.
  - **Status:** ✅ **VITÓRIA COMPLETA!** 🎉🎊🚀

**2025-12-12 (Continuação):**
- ✅ **ERRO CRÍTICO RESOLVIDO:** Aplicação não abria após correções (SyntaxError)
  - **Causa:** Faltava fechar o `if` na função `initApp` (linha 8642). Bloqueava todo o JavaScript.
  - **Sintoma:** Arquivo não carregava, console mostrava erro de sintaxe.
  - **Solução:** Adicionado fechamento `}` do `if` que verifica `typeof initApp !== 'undefined'`.
  - **Lição:** Sempre verificar fechamento de blocos após modificações. Erro de sintaxe bloqueia tudo.
  - **Prevenção:** Usar linter/formatter para detectar erros de sintaxe antes de testar.

- ✅ **ERRO CRÍTICO RESOLVIDO:** Importação não funcionava - IDs duplicados
  - **Causa:** Havia duas seções de importação com os mesmos IDs (`importDados`, `importHistorico`, `importAnotacoes`):
    1. Seção antiga `id="importar"` (oculta, linha 2165)
    2. Seção nova na aba "Backup" `id="backup"` (ativa, linha 2385)
  - **Problema:** `document.getElementById()` retornava o primeiro elemento encontrado (seção oculta), então quando usuário selecionava arquivo na aba Backup, o código não encontrava.
  - **Sintoma:** Console mostrava `dadosFile: false` mesmo com arquivo selecionado.
  - **Solução:** Removida seção antiga duplicada. Mantida apenas a seção na aba Backup.
  - **Lição:** IDs devem ser ÚNICOS em todo o documento. Nunca duplicar IDs, mesmo em seções ocultas.
  - **Prevenção:** Sempre verificar se IDs são únicos antes de criar novos elementos. Usar `querySelectorAll('#id')` para detectar duplicatas.
  - **Resultado:** ✅ Importação funcionando perfeitamente (Dados: 34 registros, Histórico: 98 registros)

**2025-12-12 (Continuação - Correções Analytics):**
- ✅ **PROBLEMA RESOLVIDO:** Performance média mostrava "1%" incorretamente
  - **Causa Raiz:** Cálculo estava correto (`Math.round(... * 100)`), mas ordem de operações podia causar arredondamento prematuro em alguns casos. Problema real: dados do histórico podem ter valores muito baixos ou formato incorreto.
  - **Solução Técnica:** Garantir multiplicação por 100 ANTES de dividir e arredondar. Código: `Math.round(historicoAtivo.reduce((sum, h) => sum + (h.rendimento || 0), 0) / historicoAtivo.length * 100)`. Adicionado log para debug: `console.log('[RESUMO] Rendimento médio calculado:', rendimentoMedio, '%')`.
  - **Por que funcionou:** Ordem de operações garante que multiplicação acontece após divisão, evitando perda de precisão. Log ajuda identificar se problema é cálculo ou dados.
  - **Prevenção:** Sempre multiplicar por 100 DEPOIS da divisão em cálculos de porcentagem. Adicionar logs para debug de cálculos críticos.
  - **Localização:** `docs/index.html` linha ~8123

- ✅ **PROBLEMA RESOLVIDO:** Gráficos não apareciam na aba Analytics (mobile)
  - **Causa Raiz:** Chart.js pode não estar carregado quando `renderAnalyticsGraficos()` é chamado, ou canvas não existe no DOM ainda (timing issue). `requestAnimationFrame` sozinho não garante que Chart.js está disponível.
  - **Solução Técnica:** Criada função `tentarRenderizarGraficos()` com retry automático:
    1. Verifica se `typeof Chart === 'undefined'` → retry após 100ms
    2. Verifica se canvas existem no DOM → retry após 100ms
    3. Só renderiza quando ambos estão prontos
  - **Código Implementado:**
    ```javascript
    const tentarRenderizarGraficos = () => {
        if (typeof Chart === 'undefined') {
            setTimeout(tentarRenderizarGraficos, 100);
            return;
        }
        const canvasBarras = document.getElementById('chartBarrasAnalytics');
        // ... verifica outros canvas
        if (!canvasBarras || !canvasLinha || !canvasRadar) {
            setTimeout(tentarRenderizarGraficos, 100);
            return;
        }
        // Renderiza quando tudo está pronto
        renderChartBarras('chartBarrasAnalytics');
        // ...
    };
    requestAnimationFrame(() => {
        setTimeout(tentarRenderizarGraficos, 50);
    });
    ```
  - **Por que funcionou:** Retry automático garante que Chart.js e DOM estão prontos antes de renderizar. Combinação de `requestAnimationFrame` + `setTimeout` + verificação explícita resolve timing issues.
  - **Prevenção:** SEMPRE verificar se bibliotecas externas (Chart.js, etc.) estão carregadas antes de usar. Usar retry pattern para timing issues no mobile.
  - **Localização:** `docs/index.html` linha ~8252

- ✅ **PROBLEMA RESOLVIDO:** Histórico vazio na aba Analytics
  - **Causa Raiz:** `renderHistoricoAnalytics()` era chamado antes do `tbody` existir no DOM. `requestAnimationFrame` sozinho não garante que HTML foi inserido.
  - **Solução Técnica:** Criada função `tentarRenderizarHistorico()` com retry automático:
    1. Verifica se `document.getElementById('historicoTableBodyAnalytics')` existe
    2. Se não existe → retry após 100ms
    3. Só renderiza quando `tbody` existe
  - **Código Implementado:**
    ```javascript
    const tentarRenderizarHistorico = () => {
        const tbody = document.getElementById('historicoTableBodyAnalytics');
        if (!tbody) {
            setTimeout(tentarRenderizarHistorico, 100);
            return;
        }
        renderHistoricoAnalytics();
    };
    requestAnimationFrame(() => {
        setTimeout(tentarRenderizarHistorico, 50);
    });
    ```
  - **Por que funcionou:** Retry garante que DOM está pronto antes de tentar acessar elementos. Mesmo padrão usado para gráficos.
  - **Prevenção:** SEMPRE verificar se elemento existe no DOM antes de usar. Usar retry pattern para elementos criados dinamicamente.
  - **Localização:** `docs/index.html` linha ~8305

- ✅ **PROBLEMA RESOLVIDO:** Áreas do Caderno e Diário não iniciavam fechadas
  - **Causa Raiz:** HTML gerado não incluía classe `collapsed` nos elementos `area-header` e `area-content`.
  - **Solução Técnica:** Adicionar `class="area-header collapsed"` e `class="area-content collapsed"` diretamente no HTML gerado por `renderCadernoV2()` e `renderDiarioPorData()`/`renderDiarioPorTema()`.
  - **Código Implementado:**
    ```javascript
    // Caderno
    html += `<div class="area-header collapsed" onclick="toggleAreaCaderno('${areaId}')">`;
    html += `<div class="area-content collapsed" id="area-content-${areaId}">`;
    
    // Diário por Data
    html += `<div class="area-header collapsed" onclick="toggleAreaCaderno('${dataId}')">`;
    html += `<div class="area-content collapsed" id="area-content-${dataId}">`;
    ```
  - **Por que funcionou:** CSS já tinha regras para `.collapsed` (max-height: 0, opacity: 0). Só faltava aplicar classe no HTML gerado.
  - **Prevenção:** Verificar se classes CSS estão sendo aplicadas no HTML gerado dinamicamente. CSS sem classe no HTML não funciona.
  - **Localização:** `docs/index.html` linha ~7891 (Caderno), ~7527 (Diário)

- ✅ **PROBLEMA RESOLVIDO:** Campo de data não existia no modal "Nova Entrada Diário"
  - **Causa Raiz:** Modal não tinha campo de data. Usuário queria poder escolher data retroativa.
  - **Solução Técnica:** Adicionado `<input type="date" id="novaDiarioData">` no modal. Função `abrirNovaEntradaDiario()` preenche com data atual por padrão. Função `salvarEntradaDiario()` usa valor do campo ao salvar. Função `editarEntradaDiario()` preenche campo ao editar.
  - **Código Implementado:**
    ```html
    <div class="form-group">
        <label class="form-label">📅 Data</label>
        <input type="date" class="form-input" id="novaDiarioData">
    </div>
    ```
    ```javascript
    // Ao abrir modal
    const hoje = new Date().toISOString().split('T')[0];
    document.getElementById('novaDiarioData').value = hoje;
    
    // Ao salvar
    const data = document.getElementById('novaDiarioData').value || hoje;
    entrada.data = data;
    ```
  - **Por que funcionou:** Input type="date" funciona nativamente no mobile. Preencher com data atual por padrão melhora UX.
  - **Prevenção:** Sempre considerar campos de data em formulários que podem precisar de datas retroativas.
  - **Localização:** `docs/index.html` linha ~1642 (HTML), ~7363 (JS abrir), ~7424 (JS salvar)

- ⚠️ **PROBLEMA PARCIALMENTE RESOLVIDO:** Cursor piscando fora dos boxes no mobile
  - **Causa Raiz:** Quando teclado aparece no mobile, viewport muda e navegador pode renderizar cursor em posição incorreta. Problema comum em iOS Safari.
  - **Solução Técnica Parcial:** Adicionado CSS:
    ```css
    .form-input:focus, .form-textarea:focus {
        position: relative;
        z-index: 1000;
        transform: translateZ(0); /* Força aceleração hardware */
        -webkit-transform: translateZ(0);
    }
    .form-input, .form-textarea {
        caret-color: var(--cobre-main); /* Define cor do cursor */
        -webkit-user-select: text;
        user-select: text;
    }
    ```
  - **Status:** ⚠️ Ainda ocorre em alguns casos. Pode precisar de ajustes adicionais ou solução diferente.
  - **Próximos Passos:** Investigar soluções específicas para iOS Safari. Pode precisar JavaScript adicional para forçar foco correto.

- ⚠️ **PROBLEMA PENDENTE:** Campo "Área" não abre lista automaticamente ao clicar
  - **Causa Raiz:** `<input type="text" list="...">` com `<datalist>` no iOS Safari só mostra opções quando usuário começa a digitar. Não abre automaticamente ao clicar/focar.
  - **Solução Necessária:** Trocar `<input list="...">` por `<select>` nativo. Select abre automaticamente ao clicar no iOS.
  - **Código Atual:** `<input type="text" id="novaDiarioArea" list="listAreasDiario">`
  - **Código Necessário:** `<select id="novaDiarioArea"><option value="">Selecione...</option>...</select>`
  - **Prevenção:** Para campos com opções pré-definidas no mobile, SEMPRE usar `<select>` em vez de `<input list="...">`. Datalist não funciona bem no iOS.

**2025-12-12 (22:06):**
- ✅ **ERRO CRÍTICO RESOLVIDO:** `SyntaxError: Cannot declare a const variable twice: 'areasExistentes'`
  - **Causa:** Declaração duplicada de `const areasExistentes` na função `atualizarTodosSelectsArea()` (linhas 8142 e 8169)
  - **Impacto:** Aplicação não abria - erro crítico que impedia uso completo
  - **Solução:** Removida segunda declaração, usando `areasCompletas` já criada anteriormente
  - **Lição:** Sempre verificar declarações de variáveis antes de adicionar novas, especialmente em funções grandes
  - **Prevenção:** Usar busca por nome da variável antes de declarar novamente

- ✅ **CORREÇÃO:** Botão "➕ Nova Área" duplicado na aba Dados
  - **Causa:** Botão foi adicionado em dois lugares diferentes durante implementação
  - **Solução:** Removido botão duplicado, mantido apenas um ao lado de "+ NOVO TEMA"
  - **Lição:** Verificar visualmente antes de considerar implementação completa

- ✅ **MELHORIA:** Integração completa de nova área em todas as abas
  - **Problema:** Nova área criada não aparecia automaticamente em todos os selects da plataforma
  - **Solução:** 
    - Adicionada atualização manual dos selects do Diário (`novaDiarioArea` e `filtroDiarioArea`)
    - Corrigidas funções `atualizarSelectsAnalise()` e `atualizarSelectsCaderno()` para incluir `AREAS_FIXAS` + áreas criadas
  - **Lição:** Quando adicionar funcionalidade, verificar TODOS os lugares onde ela deve aparecer. Integração completa é crítica.

- 📝 **PENDÊNCIA DOCUMENTADA:** Logo não aparece no MacBook
  - **Status:** Pendente (baixa prioridade)
  - **Documentação:** `DIARIO/CURSOR/PENDENCIA_LOGO_MACBOOK.md`
  - **Observação:** Funciona no iPhone, não afeta funcionalidade. Todas as tentativas técnicas documentadas.

**2025-12-13:**
- ✅ **PRINCÍPIO FUNDAMENTAL ESTABELECIDO: Organização Visual da Plataforma**
  - **Contexto:** Correção geral para eliminar espaço morto em toda a plataforma
  - **Problema Identificado:** Quando usuário expandia áreas/temas colapsáveis, conteúdo ficava confinado numa "caixinha" pequena enquanto sobrava muito espaço vazio na tela
  - **Solução Aplicada:**
    1. Aumentar `max-height` de áreas expandidas (60vh → 70vh desktop, 80vh mobile)
    2. Adicionar `min-height` quando expandido (40vh desktop, 50vh mobile)
    3. Modais ocupam quase tela toda no mobile (95vh)
    4. Containers principais com `min-height: 60vh` no mobile
    5. Tabelas sem limite de altura
    6. Função `toggleAreaCaderno` ajustada para usar 70% viewport ou scrollHeight + margem
  - **Resultado:** ✅ Conteúdo expandido ocupa espaço útil na tela, menos espaço morto, melhor aproveitamento no iPhone
  - **Documentação Criada:** `docs/PRINCIPIO_ORGANIZACAO_VISUAL_PLATAFORMA.md` - REGRA OBRIGATÓRIA
  - **Lição:** **ESPAÇO NA TELA = ESPAÇO ÚTIL** - Quando algo está ABERTO/EXPANDIDO, deve OCUPAR A TELA de forma útil, não ficar numa caixinha minúscula
  - **Prevenção:** SEMPRE aplicar padrões deste documento em novas funcionalidades. NUNCA criar elementos expandidos com altura limitada pequena. SEMPRE usar valores em `vh` para responsividade. SEMPRE testar no iPhone Safari.
  - **Commit:** `ec2a4d7` - feat: Eliminar espaço morto em toda a plataforma (VRVS v5.3)

**2025-12-13:**
- ✅ **PROBLEMA RECORRENTE RESOLVIDO:** Textos em preto aparecendo em fundo escuro (3ª ocorrência)
  - **Causa:** CSS de `.task-detail-item`, `.task-tipo`, `.task-tag-*` e `.task-meta` estava sendo sobrescrito por outros estilos ou não tinha especificidade suficiente
  - **Sintoma:** Textos "📚 área" e "Questões/flashcards" apareciam em preto nos cards da aba Tarefas, tornando-se ilegíveis
  - **Solução:** Adicionado `!important` em todas as cores dessas classes para garantir que não sejam sobrescritas:
    - `.task-detail-item { color: rgba(255, 255, 255, 0.9) !important; }`
    - `.task-tipo { color: var(--turquesa-light) !important; }`
    - `.task-tag-area { color: var(--turquesa-light) !important; }`
    - `.task-tag-rendimento { color: rgba(255,255,255,0.9) !important; }`
    - `.task-tag-tipo { color: var(--cobre-light) !important; }`
    - `.task-tag-sessoes { color: rgba(255,255,255,0.9) !important; }`
    - `.task-meta { color: rgba(255,255,255,0.9) !important; }`
    - `.task-meta * { color: rgba(255,255,255,0.9) !important; }`
  - **Lição:** Quando problema de cor persiste após múltiplas tentativas, usar `!important` para garantir especificidade máxima. Problema recorrente indica que outros estilos estão sobrescrevendo.
  - **Prevenção:** SEMPRE usar `!important` em cores críticas de elementos que aparecem em fundo escuro. Verificar especificidade CSS antes de assumir que estilo está aplicado.
  - **Commit:** `6c0d64c` - fix: Forçar cores claras em task-detail-item e task-tipo com !important
  - **Status:** ✅ Resolvido - Cores forçadas com !important garantem visibilidade

- ✅ **AJUSTES IMPLEMENTADOS:** Correções visuais na aba Tarefas e Agenda → Atrasados
  - **Ação 1:** Revertida estrutura de colapso em múltiplos níveis na aba Tarefas (Missões do Dia)
    - Removido colapso intermediário por tema
    - Cards de tema agora aparecem diretamente dentro das áreas
    - Estrutura simplificada: Área (opcional colapsável) → Cards de tema sempre visíveis
  - **Ação 2:** Implementada Opção B para Agenda → Atrasados
    - Agrupamento por área com blocos colapsáveis
    - Cores específicas para atrasados (âmbar/laranja)
    - Ordenação: primeiro por número de sessões (crescente), depois por data (mais antigo primeiro)
    - Todas as áreas começam abertas por padrão
  - **Ação 3:** Correção de cores (eliminar texto preto)
    - Varredura completa no CSS
    - Substituição de cores pretas por cores claras compatíveis com tema escuro
    - Aplicação de `!important` onde necessário
  - **Funções Criadas/Modificadas:**
    - `renderCardTemaHTML(t, hojeStr)` - Helper para renderizar card de tema
    - `renderAgendaAtrasados()` - Nova função para renderizar atrasados agrupados por área
    - `obterNumeroSessoesTema(t)` - Helper para obter número de sessões
    - `renderCardTemaAtrasadoHTML(t)` - Helper para renderizar card de tema atrasado
    - `toggleAtrasadosArea(areaId)` - Função para colapso de áreas atrasadas
  - **CSS Adicionado:**
    - `.atrasados-area`, `.atrasados-area-header`, `.atrasados-area-nome`, `.atrasados-area-badge`, `.atrasados-area-conteudo`
    - Cores específicas para indicar atraso (rgba(255,159,64,...))
  - **Lição:** Quando usuário pede "reverter" ou "manter estrutura antiga", significa simplificar, não complicar. Estrutura antiga funcionava melhor.
  - **Prevenção:** SEMPRE manter estrutura simples quando possível. Colapso em múltiplos níveis piora UX. Agrupar por área é suficiente.
  - **Status:** ✅ Implementado e testado

**2025-12-13 (Continuação - PROTOCOLO PATCH LIMPO):**
- ✅ **PROTOCOLO INTERNO CRIADO:** PROTOCOLO_PATCH_LIMPO_VRVS.md
  - **Objetivo:** Garantir patches mínimos, cirúrgicos e tecnicamente justificados
  - **Regras críticas:**
    - Diagnóstico antes de solução
    - Remover conflitos antes de adicionar regras
    - `!important` é último recurso
    - Não adicionar `transform` sem evidência clara
    - Manter design system e semântica
  - **Proibido:** Blocos gigantes de `!important`, hacks sem necessidade, alterar múltiplas abas de uma vez
  - **Commit:** `74cdcc7`, `aa60d21`, `575e649`
  - **Status:** ✅ Protocolo ativo e sendo seguido

- ✅ **CORREÇÃO UX MODAL DIÁRIO:** Campo Tópico e cursor iOS
  - **Problema:** Campo Tópico muito pequeno + cursor piscando fora do box no iOS
  - **Solução (PROTOCOLO PATCH LIMPO):**
    1. Removida regra conflitante: `#modalNovaDiario .form-textarea { min-height: 300px !important; }`
    2. Definida regra completa para `#novaDiarioTopico` com todas as propriedades necessárias
    3. Adicionada função `autoResizeTextarea()` para expandir conforme digita
    4. Adicionada regra separada para `#novaDiarioResposta`
  - **NÃO FEITO (conforme protocolo):**
    - ❌ Bloco gigante de `!important`
    - ❌ `transform: translateZ(0)` no modal
    - ❌ `position: relative`, `left: 0` sem evidência
  - **Commit:** `37870a8`
  - **Lição:** Seguir PROTOCOLO PATCH LIMPO garante soluções limpas. Remover conflitos primeiro, depois definir regras específicas.
  - **Status:** ✅ Resolvido seguindo protocolo rigorosamente

- ✅ **BUG CRÍTICO RESOLVIDO:** Checkbox "revisar amanhã" invisível no iPhone
  - **Problema:** Checkbox não aparecia no iPhone Safari (texto sim, checkbox não)
  - **Causa:** Style inline dependia do estilo nativo do iOS, que não funcionava dentro do box âmbar
  - **Solução (PROTOCOLO PATCH LIMPO):**
    1. Removidos styles inline do checkbox e container
    2. Criadas classes CSS limpas: `.diario-atencao-container`, `.diario-atencao-checkbox`, `.diario-atencao-label`
    3. Checkbox agora tem borda própria (`border: 2px solid var(--cobre-main)`) visível em qualquer plataforma
    4. Estado `:checked` com `box-shadow` para feedback visual
  - **Varredura de remendos:** Verificados estilos inline multi-linha - todos completos e corretos. Nenhum remendo similar encontrado.
  - **Garantias:** Lógica JS não alterada, estrutura de dados preservada, apenas visual corrigido
  - **Commit:** `2d208f9`
  - **Lição:** Remover styles inline quebrados e criar CSS limpo é mais eficaz que adicionar hacks. Checkbox precisa de borda própria para ser visível em iOS.
  - **Status:** ✅ Resolvido - Checkbox visível e funcional

═══════════════════════════════════════════════════════════════

