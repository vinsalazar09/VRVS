# docs/ — PWA (deploy)

| Item | Nota |
|------|------|
| `index.html` | App principal |
| `sw.js` | Service worker; `CACHE_NAME` define revisão no iPhone |
| `manifest.json`, `favicon.ico`, `logo.*` | Instalação / ícones |
| `mindmap/` | Sub-app em cache pelo SW |
| `recover.html` | Ferramenta de recuperação listada no SW |
| `MANUAL_VRVS_v3.html` | Manual do utilizador (consulta); em cache pelo SW |
| `*.md` aqui | Só documentação mínima canónica + `COMO_ABRIR_CONSOLE_IPHONE.md` |

GitHub Pages: raiz do site = esta pasta.
